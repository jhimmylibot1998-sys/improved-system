# Video assets

Add `.mp4` or `.webm` files here. The current portfolio includes `my-ai-video.mp4`, which is linked from `ai-videos.html`.

Use lowercase filenames with hyphens and update the matching gallery card when adding new work.
