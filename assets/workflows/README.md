# Workflow assets

Add workflow references here as `.pdf`, `.png`, or `.jpg` files. Update the matching card in `automation-workflows.html` so the asset is discoverable from the public gallery.

Use lowercase filenames with hyphens and forward-slash paths in HTML.
